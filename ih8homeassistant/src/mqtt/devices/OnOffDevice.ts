/**
 * On/Off devices: switches and basic lights
 * Supports: OnOffPlugInUnitDevice, OnOffLightDevice
 */

import { BaseDevice } from "./BaseDevice.js";

export class OnOffDevice extends BaseDevice {
    // This class only needs the base functionality
    // All on/off logic is handled in BaseDevice
}
